# Project Rules

## Git & Deployment
- **Automatic Push**: Upon completion of every task, changes must be pushed to GitHub to trigger Vercel deployment.
- **Command Sequence**:
  1. `git add .`
  2. `git commit -m "..."`
  3. `git push`
